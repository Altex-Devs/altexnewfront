import { AboutHeader, AboutBody, AboutReportPerformance } from "../about"

export const About = () => {
    return (
        <div>
            <AboutHeader/>
            <AboutBody />
            <AboutReportPerformance />
        </div>
    )
}