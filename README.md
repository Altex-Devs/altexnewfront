Altex homepage development
==========================

Uses Tailwind for styling.

Start dev server on port 3000:

    npm start

Build to `build` folder:

    npm run build

Surge deploy:

    cd build
    cp index.html 200.html
    surge